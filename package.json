{
  "name": "nodejs-crypto",
  "version": "0.1.0",
  "description": "nodejs-crypto",
  "main": "crypto.js",
  "scripts": {
    "test": "echo \"Error: no test specified! Configure in package.json\" && exit 1"
  }
}
